export default function FeaturesSection() {
  const features = [
    { icon: "📱", title: "Responsive Layouts" },
    { icon: "⚙️", title: "Reusable Components" },
    { icon: "🔤", title: "Clean Typography" },
    { icon: "📏", title: "Consistent Spacing" },
    { icon: "♿", title: "Accessible Design" },
    { icon: "⚡", title: "Fast Prototyping" },
  ];
  return (
    <section className="max-w-6xl mx-auto px-4 py-16">
      <div className="text-center mb-12">
        <h2 className="text-3xl font-semibold">Balanced Layouts</h2>
        <p className="mt-3 text-gray-600">
          Clean grids and even spacing make your UI shine.
        </p>
      </div>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        {features.map((f) => (
          <div
            key={f.title}
            className="rounded-2xl border p-6 hover:shadow-md transition flex flex-col gap-3"
          >
            <div className="text-3xl">{f.icon}</div>
            <h3 className="text-xl font-semibold">{f.title}</h3>
            <p className="text-gray-600 leading-relaxed">
              Lorem ipsum dolor sit amet consectetur.
            </p>
          </div>
        ))}
      </div>
    </section>
  );
}
